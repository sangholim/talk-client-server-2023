const spinner = {
    show: function() {
        var spinner = document.getElementById('components-spinner');
            spinner.style.display  = "";
    },
    hide: function() {
        var spinner = document.getElementById('components-spinner');
            spinner.style.display  = "none";
    }
}